<%= form_for @member, :remote => true do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
	<%= f.hidden_field :family_id, :value => current_user.family.id %>
	
  <p><%= f.submit %></p>
<% end %>
